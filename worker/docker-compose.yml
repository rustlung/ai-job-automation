services:
  api:
    build:
      context: ./api
      dockerfile: Dockerfile
    container_name: ai-job-worker-api
    restart: unless-stopped
    env_file:
      - path: ./api/.env
        required: false
    volumes:
      - ./secrets:/run/secrets/hh:ro
    ports:
      - "8001:8000"
